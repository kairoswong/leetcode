// Auto-generated by scripts/generate-index.py
// Full solution data with code.
const SOLUTIONS_DATA = [
  {
    "id": 1,
    "title": "Two Sum",
    "file": "1.two-sum.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Given an array of integers nums and an integer target, return indices of the two numbers that add up to target.",
    "approach": "Use a hash map to store each number's index as we iterate. For each number, compute its complement (target \u2212 num) and check if it already exists in the map. This gives O(1) lookups per element.",
    "keyInsight": "A hash map trades space for time \u2014 we store past elements to avoid the O(n\u00b2) brute-force comparison.",
    "tags": [
      "array",
      "hash-map"
    ],
    "code": "class Solution:\n    def twoSum(self, nums: List[int], target: int) -> List[int]:\n        hash_map = {}\n        for index, num in enumerate(nums):\n            complement = target - num\n            if complement in hash_map:\n                return [hash_map[complement], index]\n            hash_map[num] = index\n\n        return []"
  },
  {
    "id": 2,
    "title": "Add Two Numbers",
    "file": "2.add-two-numbers.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(max(m,n))",
      "space": "O(max(m,n))"
    },
    "desc": "Add two numbers represented by linked lists and return the sum as a linked list.",
    "approach": "Traverse both lists simultaneously, summing digit-by-digit with carry. Create a new node for each digit of the result.",
    "keyInsight": "Use a carry variable to handle sums \u2265 10, continuing to the next most significant digit.",
    "tags": [
      "math",
      "linked-list"
    ],
    "code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\n\nclass Solution:\n    def addTwoNumbers(self, l1: ListNode | None, l2: ListNode | None) -> ListNode | None:\n        dummy_head = ListNode(0)\n        current = dummy_head\n        carry = 0\n\n        while l1 or l2 or carry:\n            val1 = l1.val if l1 else 0\n            val2 = l2.val if l2 else 0\n            total = val1 + val2 + carry\n\n            carry = total // 10\n            current.next = ListNode(total % 10)\n            current = current.next\n\n            if l1:\n                l1 = l1.next\n            if l2:\n                l2 = l2.next\n\n        return dummy_head.next"
  },
  {
    "id": 3,
    "title": "Longest Substring Without Repeating Characters",
    "file": "3.longest-substring-without-repeating-characters.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(min(m,n))"
    },
    "desc": "Find the length of the longest substring without repeating characters.",
    "approach": "Use a sliding window with two pointers (left, right). Expand the right pointer, and when a duplicate is found, move the left pointer past the previous occurrence.",
    "keyInsight": "The sliding window ensures each character is visited at most twice \u2014 once by each pointer \u2014 achieving O(n) time.",
    "tags": [
      "string",
      "sliding-window",
      "hash-map"
    ],
    "code": "class Solution:\n    def lengthOfLongestSubstring(self, s: str) -> int:\n        char_map = {}\n        left = 0\n        max_length = 0\n\n        for right, char in enumerate(s):\n            if char in char_map and char_map[char] >= left:\n                left = char_map[char] + 1\n            char_map[char] = right\n            max_length = max(max_length, right - left + 1)\n        \n        return max_length"
  },
  {
    "id": 4,
    "title": "Median of Two Sorted Arrays",
    "file": "4.median-of-two-sorted-arrays.py",
    "difficulty": "hard",
    "status": "solved",
    "complexity": {
      "time": "O(log(min(m,n)))",
      "space": "O(1)"
    },
    "desc": "Find the median of two sorted arrays in O(log(m+n)) time.",
    "approach": "Binary search on the smaller array to partition both arrays such that all left elements \u2264 all right elements. The median is derived from the partition boundary.",
    "keyInsight": "By partitioning the smaller array, we implicitly partition the larger one \u2014 achieving O(log(min(m,n))) instead of O(m+n).",
    "tags": [
      "binary-search",
      "divide-and-conquer"
    ],
    "code": "class Solution:\n    def findMedianSortedArrays(self, nums1: List[int], nums2: List[int]) -> float:\n        def getKth(k):\n            i = j = 0\n            while True:\n                if i == len(nums1): return nums2[j + k]\n                if j == len(nums2): return nums1[i + k]\n                if k == 0: return min(nums1[i], nums2[j])\n                \n                half = (k + 1) // 2\n                ni = min(i + half, len(nums1))\n                nj = min(j + half, len(nums2))\n                \n                if nums1[ni - 1] < nums2[nj - 1]:\n                    k -= ni - i\n                    i = ni\n                else:\n                    k -= nj - j\n                    j = nj\n        \n        total = len(nums1) + len(nums2)\n        if total % 2 == 1:\n            return getKth(total // 2)\n        else:\n            return (getKth(total//2 - 1) + getKth(total//2)) / 2"
  },
  {
    "id": 5,
    "title": "Longest Palindromic Substring",
    "file": "5.longest-palindromic-substring.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n\u00b2)",
      "space": "O(1)"
    },
    "desc": "Find the longest palindromic substring in a given string.",
    "approach": "Expand around the center \u2014 for each position (and between each pair), expand outward as long as the characters match, tracking the longest palindrome found.",
    "keyInsight": "There are 2n\u22121 possible centers in a string of length n (including gaps between characters). Expanding from each center is O(n) per center.",
    "tags": [
      "string",
      "sliding-window",
      "hash-map"
    ],
    "code": "# Manacher Algorithm is all you need\nclass Solution:\n    def longestPalindrome(self, s: str) -> str:\n        processed_s = \"/\" + \"/\".join(s) + \"/\"\n        n = len(processed_s)\n\n        p = [0] * n\n        mid, r = 0, 0\n        max_center, max_radius = 0, 0\n\n        for i in range(n):\n            if i < r:\n                p[i] = min(p[2 * mid - i], r - i)\n            else:\n                p[i] = 1\n\n            while (i - p[i] >= 0 and \n                i + p[i] < n and\n                processed_s[i - p[i]] == processed_s[i + p[i]]):\n                p[i] += 1\n\n            if i + p[i] > r:\n                mid = i\n                r = i +p[i]\n            \n            if p[i] > max_radius:\n                max_radius = p[i]\n                max_center = i\n\n        start = (max_center - max_radius + 1) // 2\n        length = max_radius - 1\n\n        return s[start : start + length]"
  },
  {
    "id": 6,
    "title": "Zigzag Conversion",
    "file": "6.zigzag-conversion.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Convert a string into a zigzag pattern on a given number of rows.",
    "approach": "Simulate the zigzag traversal by iterating through the string and appending each character to the appropriate row, changing direction at the top and bottom rows.",
    "keyInsight": "Track direction (down/up) and flip when reaching row 0 or the last row \u2014 this gives an O(n) one-pass solution.",
    "tags": [
      "string",
      "simulation"
    ],
    "code": "class Solution:\n    def convert(self, s: str, numRows: int) -> str:\n        if numRows == 1 or numRows >= len(s):\n            return s\n\n        rows = [''] * numRows\n        cur = 0\n        down = False\n\n        for c in s:\n            rows[cur] += c\n            if cur == 0 or cur == numRows - 1:\n                down = not down\n            cur += 1 if down else -1\n\n        return ''.join(rows)"
  },
  {
    "id": 7,
    "title": "Reverse Integer",
    "file": "7.reverse-integer.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(log n)",
      "space": "O(1)"
    },
    "desc": "Reverse digits of a 32-bit signed integer with overflow handling.",
    "approach": "Repeatedly pop the last digit using modulo 10 and push it onto the result. Check for overflow before updating the result.",
    "keyInsight": "Check overflow before it happens: if rev > INT_MAX/10 or rev < INT_MIN/10, the next digit will cause overflow.",
    "tags": [
      "math",
      "simulation"
    ],
    "code": "class Solution:\n    def reverse(self, x: int) -> int:\n        INT_MIN, INT_MAX = -2**31, 2**31 - 1\n\n        sign = 1 if x >= 0 else -1\n        x = abs(x)\n        reversed_x = 0\n\n        while x > 0:\n            digit = x % 10\n            reversed_x = reversed_x * 10 + digit\n            x //= 10\n\n        result = sign * reversed_x\n\n        if result < INT_MIN or result > INT_MAX:\n            return 0\n\n        return result"
  },
  {
    "id": 8,
    "title": "String to Integer (atoi)",
    "file": "8.string-to-integer-atoi.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(1)"
    },
    "desc": "Implement the myAtoi(string s) function that converts a string to a 32-bit signed integer.",
    "approach": "Skip leading whitespace, handle sign character, then read digits while checking for overflow at each step.",
    "keyInsight": "Process the string character by character with a state machine: whitespace \u2192 sign \u2192 digits \u2192 end \u2014 early exit on non-digit.",
    "tags": [
      "string",
      "simulation"
    ],
    "code": "class Solution:\n    def myAtoi(self, s: str) -> int:\n        INT_MIN, INT_MAX = -2**31, 2**31 - 1\n        i, n = 0, len(s)\n\n        while i < n and s[i] == ' ':\n            i += 1\n\n        if i >= n:\n            return 0\n\n        sign = 1\n        if s[i] in '+-':\n            sign = -1 if s[i] == '-' else 1\n            i += 1\n\n        result = 0\n        while i < n and s[i].isdigit():\n            digit = int(s[i])\n            if result > (INT_MAX - digit) // 10:\n                return INT_MAX if sign == 1 else INT_MIN\n            result = result * 10 + digit\n            i += 1\n\n        return sign * result"
  },
  {
    "id": 9,
    "title": "Palindrome Number",
    "file": "9.palindrome-number.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Determine whether an integer is a palindrome.",
    "approach": "Convert the integer to a string and check if the string is equal to its reverse.",
    "keyInsight": "Strings in Python can be easily reversed using slicing [::-1], making this a concise approach.",
    "tags": [
      "math",
      "string"
    ],
    "code": "class Solution:\n    def isPalindrome(self, x: int) -> bool:\n        if x < -2 ** 31 or x > 2 ** 31 - 1:\n            return False\n        \n        s = str(x)\n        return s == s[::-1]"
  },
  {
    "id": 21,
    "title": "Merge Two Sorted Lists",
    "file": "21.merge-two-sorted-lists.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(m+n)",
      "space": "O(1)"
    },
    "desc": "Merge two sorted linked lists into one sorted list.",
    "approach": "Use a dummy head node and a pointer. Compare the current nodes of both lists, appending the smaller one, and advancing the pointer.",
    "keyInsight": "A dummy head eliminates edge-case handling for the initial empty result list, making the code cleaner and less error-prone.",
    "tags": [
      "linked-list",
      "recursion"
    ],
    "code": "# Definition for singly-linked list.\n# class ListNode:\n#     def __init__(self, val=0, next=None):\n#         self.val = val\n#         self.next = next\nclass Solution:\n    # def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\n    #     dummy = ListNode()\n    #     current = dummy\n        \n    #     while list1 and list2:\n    #         if list1.val < list2.val:\n    #             current.next = list1\n    #             list1 = list1.next\n    #         else:\n    #             current.next = list2\n    #             list2 = list2.next\n    #         current = current.next\n        \n    #     current.next = list1 or list2\n        \n    #     return dummy.next\n\n    # Alternative recursive solution:\n    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\n        if not list1:\n            return list2\n        if not list2:\n            return list1\n        \n        if list1.val < list2.val:\n            list1.next = self.mergeTwoLists(list1.next, list2)\n            return list1\n        else:\n            list2.next = self.mergeTwoLists(list1, list2.next)\n            return list2"
  },
  {
    "id": 28,
    "title": "Find the Index of the First Occurrence in a String",
    "file": "28.find-the-index-of-the-first-occurrence-in-a-string.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n*m)",
      "space": "O(1)"
    },
    "desc": "Return the index of the first occurrence of needle in haystack, or -1 if not found.",
    "approach": "Slide a window of length needle across haystack, comparing character by character.",
    "keyInsight": "Only need to search up to (len(haystack) \u2212 len(needle)) to avoid unnecessary comparisons.",
    "tags": [
      "string",
      "two-pointer"
    ],
    "code": "class Solution:\n    def strStr(self, haystack: str, needle: str) -> int:\n        length = len(needle)\n        kmp = [0] * length\n        for i in range(1, length):\n            j = kmp[i - 1]\n            while j > 0 and needle[i] != needle[j]:\n                j = kmp[j - 1]\n            if needle[i] == needle[j]:\n                j += 1\n            kmp[i] = j\n\n        j = 0\n        for i in range(len(haystack)):\n            while j > 0 and haystack[i] != needle[j]:\n                j = kmp[j - 1]\n            if haystack[i] == needle[j]:\n                j += 1\n            if j == length:\n                return i - length + 1\n        return -1"
  },
  {
    "id": 36,
    "title": "Valid Sudoku",
    "file": "36.valid-sudoku.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(1)",
      "space": "O(1)"
    },
    "desc": "Determine if a 9x9 Sudoku board is valid according to Sudoku rules.",
    "approach": "Use a hash set to track seen numbers in each row, column, and 3\u00d73 box. The box index is derived from (row//3)*3 + (col//3).",
    "keyInsight": "A flat set with encoded keys like `(5) in row 1` lets us validate all three constraints in a single pass through the board.",
    "tags": [
      "hash-map",
      "matrix",
      "simulation"
    ],
    "code": "class Solution:\n    def isValidSudoku(self, board: List[List[str]]) -> bool:\n        seen = set()\n\n        for row in range(9):\n            for col in range(9):\n                num = board[row][col]\n                if num == '.':\n                    continue\n\n                row_key = f\"({num}) in row {row}\"\n                col_key = f\"({num}) in column {col}\"\n                box_key = f\"({num}) in box {(row // 3) * 3 + (col // 3)}\"\n\n                if row_key in seen or col_key in seen or box_key in seen:\n                    return False\n\n                seen.add(row_key)\n                seen.add(col_key)\n                seen.add(box_key)\n\n        return True"
  },
  {
    "id": 42,
    "title": "Trapping Rain Water",
    "file": "42.trapping-rain-water.py",
    "difficulty": "hard",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(1)"
    },
    "desc": "Compute how much water can be trapped between bars given an elevation map.",
    "approach": "Two-pointer approach from both ends. Track left_max and right_max \u2014 water trapped at a position depends on the shorter of the two max heights.",
    "keyInsight": "At any position, water trapped = min(max left height, max right height) \u2212 current height. The two-pointer method computes this in one pass.",
    "tags": [
      "two-pointer",
      "array",
      "stack"
    ],
    "code": "class Solution:\n    def trap(self, height: List[int]) -> int:\n        left, right = 0, len(height) - 1\n        left_max = right_max = water = 0\n\n        while left < right:\n            if height[left] < height[right]:\n                if height[left] >= left_max:\n                    left_max = height[left]\n                else:\n                    water += left_max - height[left]\n                left += 1\n            else:\n                if height[right] >= right_max:\n                    right_max = height[right]\n                else:\n                    water += right_max - height[right]\n                right -= 1\n\n        return water"
  },
  {
    "id": 48,
    "title": "Rotate Image",
    "file": "48.rotate-image.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n\u00b2)",
      "space": "O(1)"
    },
    "desc": "Rotate an n\u00d7n 2D matrix 90 degrees clockwise in-place.",
    "approach": "First transpose the matrix (swap [i][j] with [j][i]), then reverse each row. This achieves a 90\u00b0 clockwise rotation in-place.",
    "keyInsight": "Transpose + reverse each row is a cleaner mental model than rotating four cells at a time \u2014 both are O(n\u00b2) but this is far less error-prone.",
    "tags": [
      "matrix",
      "simulation"
    ],
    "code": "class Solution:\n    def rotate(self, matrix: List[List[int]]) -> None:\n        \"\"\"\n        Do not return anything, modify matrix in-place instead.\n        \"\"\"\n        n = len(matrix)\n\n        for i in range(n):\n            for j in range(i + 1, n):\n                matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]\n\n        for i in range(n):\n            matrix[i].reverse()"
  },
  {
    "id": 69,
    "title": "Sqrt(x)",
    "file": "69.sqrt-x.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(log n)",
      "space": "O(1)"
    },
    "desc": "Return the square root of x rounded down to the nearest integer.",
    "approach": "Binary search for the square root in range [0, x]. At each step, compute mid\u00b2 and compare with x.",
    "keyInsight": "Binary search on a monotonic function (the square of a number is monotonically increasing for non-negative inputs).",
    "tags": [
      "binary-search",
      "math"
    ],
    "code": "class Solution:\n    def mySqrt(self, x: int) -> int:\n        if x < 2:\n            return x\n        \n        left, right = 2, x // 2\n        while left <= right:\n            pivot = left + (right - left) // 2\n            num = pivot * pivot\n            \n            if num > x:\n                right = pivot - 1\n            elif num < x:\n                left = pivot + 1\n            else:\n                return pivot\n\n        return right"
  },
  {
    "id": 70,
    "title": "Climbing Stairs",
    "file": "70.climbing-stairs.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(1)"
    },
    "desc": "You are climbing a staircase. In how many distinct ways can you climb to the top?",
    "approach": "This is the Fibonacci sequence in disguise. The number of ways to reach step n = ways to reach n-1 + ways to reach n-2.",
    "keyInsight": "Base cases: 1 way to reach stair 1 (1 step), 2 ways to reach stair 2 (1+1 or 2). Each subsequent stair is just the sum of the previous two.",
    "tags": [
      "dp",
      "math"
    ],
    "code": "class Solution:\n    def climbStairs(self, n: int) -> int:\n        if n <= 2:\n            return n\n        a, b = 1, 2\n        \n        for _ in range(3, n + 1):\n            a, b = b, a + b\n            \n        return b"
  },
  {
    "id": 102,
    "title": "Binary Tree Level Order Traversal",
    "file": "102.binary-tree-level-order-traversal.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Given the root of a binary tree, return the level order traversal of its nodes' values (i.e., from left to right, level by level).",
    "approach": "Use a queue to perform BFS (breadth-first search). Process nodes level by level: capture the current queue size, pop that many nodes, collect their values, and enqueue their children.",
    "keyInsight": "A queue lets us process nodes in FIFO order, ensuring we visit children in left-to-right sequence within each level.",
    "tags": [
      "tree",
      "bfs"
    ],
    "code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def levelOrder(self, root: Optional[TreeNode]) -> List[List[int]]:\n        result = []\n        if not root:\n            return result\n\n        queue = [root]\n        while queue:\n            level_size = len(queue)\n            level = []\n            for _ in range(level_size):\n                node = queue.pop(0)\n                level.append(node.val)\n                if node.left:\n                    queue.append(node.left)\n                if node.right:\n                    queue.append(node.right)\n            result.append(level)\n\n        return result"
  },
  {
    "id": 104,
    "title": "Maximum Depth of Binary Tree",
    "file": "104.maximum-depth-of-binary-tree.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Given the root of a binary tree, return its maximum depth.",
    "approach": "Use DFS recursion: if the node is null, return 0; otherwise return 1 plus the maximum depth of the left and right subtrees.",
    "keyInsight": "The depth of a tree is defined recursively: a null node contributes 0, and a non-null node contributes 1 plus the maximum depth of its subtrees.",
    "tags": [
      "tree",
      "bfs"
    ],
    "code": "# Definition for a binary tree node.\n# class TreeNode:\n#     def __init__(self, val=0, left=None, right=None):\n#         self.val = val\n#         self.left = left\n#         self.right = right\nclass Solution:\n    def maxDepth(self, root: Optional[TreeNode]) -> int:\n        if not root:\n            return 0\n\n        left_depth = self.maxDepth(root.left)\n        right_depth = self.maxDepth(root.right)\n        return 1 + max(left_depth, right_depth)"
  },
  {
    "id": 118,
    "title": "Pascal's Triangle",
    "file": "118.pascals-triangle.py",
    "difficulty": "easy",
    "status": "solved",
    "complexity": {
      "time": "O(n\u00b2)",
      "space": "O(n\u00b2)"
    },
    "desc": "Given an integer numRows, return the first numRows of Pascal's triangle.",
    "approach": "Build the triangle row by row. Each row starts and ends with 1, and each interior element is the sum of the two elements above it.",
    "keyInsight": "Each row can be initialized with all 1s, then the inner elements (excluding first and last) are filled using values from the previous row \u2014 this avoids special-case handling for the edges.",
    "tags": [
      "math",
      "array",
      "dp"
    ],
    "code": "class Solution:\n    def generate(self, numRows: int) -> List[List[int]]:\n        triangle = []\n\n        for i in range(numRows):\n            row = [1] * (i + 1)\n            for j in range(1, i):\n                row[j] = triangle[i - 1][j - 1] + triangle[i - 1][j]\n            triangle.append(row)\n\n        return triangle"
  },
  {
    "id": 149,
    "title": "Max Points on a Line",
    "file": "149.max-points-on-a-line.py",
    "difficulty": "hard",
    "status": "solved",
    "complexity": {
      "time": "O(n\u00b2)",
      "space": "O(n)"
    },
    "desc": "Given n points on a plane, return the maximum number of points that lie on the same straight line.",
    "approach": "For each point, compute normalized slopes to every other point using a hash map. Count how many points share each slope, and keep the maximum line count.",
    "keyInsight": "Normalizing slopes makes collinear points collapse into the same bucket, and handling duplicates separately prevents them from being lost.",
    "tags": [
      "hash-map",
      "geometry",
      "math"
    ],
    "code": "class Solution:\n    def maxPoints(self, points: List[List[int]]) -> int:\n        def gcd(a: int, b: int) -> int:\n            while b:\n                a, b = b, a % b\n            return abs(a)\n\n        if len(points) < 3:\n            return len(points)\n\n        max_points = 1\n\n        for i in range(len(points)):\n            slopes = {}\n            same = 1\n            x1, y1 = points[i]\n\n            for j in range(i + 1, len(points)):\n                x2, y2 = points[j]\n                dx = x2 - x1\n                dy = y2 - y1\n\n                if dx == 0 and dy == 0:\n                    same += 1\n                    continue\n\n                if dx == 0:\n                    slope = \"inf\"\n                else:\n                    g = gcd(abs(dx), abs(dy))\n                    dx //= g\n                    dy //= g\n                    if dx < 0:\n                        dx, dy = -dx, -dy\n                    slope = (dx, dy)\n\n                slopes[slope] = slopes.get(slope, 0) + 1\n\n            max_points = max(max_points, same + max(slopes.values(), default=0))\n\n        return max_points"
  },
  {
    "id": 151,
    "title": "Reverse Words in a String",
    "file": "151.reverse-words-in-a-string.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Given a string, reverse the order of the words while removing extra spaces.",
    "approach": "Extract the words from the string, reverse their order, and join them back with single spaces.",
    "keyInsight": "The key is to treat consecutive spaces as one separator and to reverse the word list after extraction.",
    "tags": [
      "string",
      "two-pointer"
    ],
    "code": "class Solution:\n    def reverseWords(self, s: str) -> str:\n        # words = s.split()\n        # return \" \".join(reversed(words))\n        words = []\n        i = 0\n        n = len(s)\n\n        while i < n:\n            while i < n and s[i] == ' ':\n                i += 1\n            if i >= n:\n                break\n\n            j = i\n            while j < n and s[j] != ' ':\n                j += 1\n\n            words.append(s[i:j])\n            i = j\n\n        return \" \".join(reversed(words))"
  },
  {
    "id": 165,
    "title": "Compare Version Numbers",
    "file": "165.compare-version-numbers.py",
    "difficulty": "medium",
    "status": "solved",
    "complexity": {
      "time": "O(n)",
      "space": "O(n)"
    },
    "desc": "Compare two version numbers version1 and version2, return -1, 0, or 1 based on their comparison.",
    "approach": "Split both version strings by '.', find the max length, then iterate through indices. For each index, parse the revision part as int (defaulting to 0 if the version has fewer parts). Compare pairwise, returning -1 or 1 on the first difference.",
    "keyInsight": "By using max(len(...)) and defaulting missing parts to 0 via index checks, we handle unequal-length versions without needing zip_longest or itertools.",
    "tags": [
      "string",
      "simulation"
    ],
    "code": "class Solution:\n    def compareVersion(self, version1: str, version2: str) -> int:\n        parts_1 = version1.split(\".\")\n        parts_2 = version2.split(\".\")\n\n        n = max(len(parts_1), len(parts_2))\n        for i in range(n):\n            a = int(parts_1[i]) if i < len(parts_1) else 0\n            b = int(parts_2[i]) if i < len(parts_2) else 0\n\n            if a < b:\n                return -1\n            if a > b:\n                return 1\n\n        return 0"
  }
];
