{
  "1":  "A hash map trades space for time — we store past elements to avoid the O(n²) brute-force comparison.",
  "2":  "Use a carry variable to handle sums ≥ 10, continuing to the next most significant digit.",
  "3":  "The sliding window ensures each character is visited at most twice — once by each pointer — achieving O(n) time.",
  "4":  "By partitioning the smaller array, we implicitly partition the larger one — achieving O(log(min(m,n))) instead of O(m+n).",
  "5":  "There are 2n−1 possible centers in a string of length n (including gaps between characters). Expanding from each center is O(n) per center.",
  "6":  "Track direction (down/up) and flip when reaching row 0 or the last row — this gives an O(n) one-pass solution.",
  "7":  "Check overflow before it happens: if rev > INT_MAX/10 or rev < INT_MIN/10, the next digit will cause overflow.",
  "8":  "Process the string character by character with a state machine: whitespace → sign → digits → end — early exit on non-digit.",  "9": "Strings in Python can be easily reversed using slicing [::-1], making this a concise approach.",  "21": "A dummy head eliminates edge-case handling for the initial empty result list, making the code cleaner and less error-prone.",
  "28": "Only need to search up to (len(haystack) − len(needle)) to avoid unnecessary comparisons.",
  "36": "A flat set with encoded keys like `(5) in row 1` lets us validate all three constraints in a single pass through the board.",
  "42": "At any position, water trapped = min(max left height, max right height) − current height. The two-pointer method computes this in one pass.",
  "48": "Transpose + reverse each row is a cleaner mental model than rotating four cells at a time — both are O(n²) but this is far less error-prone.",
  "69": "Binary search on a monotonic function (the square of a number is monotonically increasing for non-negative inputs).",
  "70": "Base cases: 1 way to reach stair 1 (1 step), 2 ways to reach stair 2 (1+1 or 2). Each subsequent stair is just the sum of the previous two.",
  "102": "A queue lets us process nodes in FIFO order, ensuring we visit children in left-to-right sequence within each level.",
  "104": "The depth of a tree is defined recursively: a null node contributes 0, and a non-null node contributes 1 plus the maximum depth of its subtrees.",
  "118": "Each row can be initialized with all 1s, then the inner elements (excluding first and last) are filled using values from the previous row — this avoids special-case handling for the edges.",
  "149": "Normalizing slopes makes collinear points collapse into the same bucket, and handling duplicates separately prevents them from being lost.",
  "151": "The key is to treat consecutive spaces as one separator and to reverse the word list after extraction.",
  "165": "By using max(len(...)) and defaulting missing parts to 0 via index checks, we handle unequal-length versions without needing zip_longest or itertools.",
  "190": "Bit manipulation operates directly on binary representation — each n & 1 peels off the lowest bit, and shifting builds the reversed result one bit at a time, all in constant time and space.",
  "191": "n & 1 isolates the least significant bit; repeatedly peeling off bits and counting the 1s gives the Hamming weight directly.",
  "206": "Reversing a linked list iteratively is O(n) with O(1) space — each node's next pointer is redirected to the previous node in a single pass.",
  "207": "If a cycle exists in the directed graph, some nodes will never reach indegree 0, making it impossible to topologically sort all courses — the queue empties before processing all nodes.",
  "215": "Maintaining a heap of exactly k elements ensures we keep only the k largest elements seen so far, and the smallest of those is the kth largest overall.",
  "221": "The size of the largest square ending at (i, j) is limited by the smallest neighboring square above, to the left, and diagonally above-left — the minimum of these three plus one gives the new side length."
}
